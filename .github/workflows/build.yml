name: main build

on:
  push:
    branches:
      - main

jobs:
  build_python_3_12:
    runs-on: ubuntu-latest
    steps:
# check out the github repository
    - uses: actions/checkout@v7.0.0
# set up python
    - name: set up python "3.12"
      uses: actions/setup-python@v6.3.0
      with:
        python-version: "3.12"
# clean-up
    - name: clean up
      run: |
        rm -rf /home/runner/work/warehouse/warehouse/docs/build/
        rm -rf /home/runner/work/warehouse/warehouse/dist
        rm -rf /home/runner/work/warehouse/warehouse/build
# execute the actual make build process
    - name: execute the make build
      run: ./make.sh
# remove warehouse
    - name: purge local warehouse installation
      run: |
        pip uninstall -y warehouse
# attempt to install it again from github
    - name: install warehouse from github
      run: |
        pip install git+https://github.com/thomasWeise/warehouse.git
        python -c "import warehouse"
# fix urls in documentation
    - name: fix documentation urls
      run: |
        find /home/runner/work/warehouse/warehouse/docs/build/ \( -type d -name .git -prune \) -o -type f -name "*.html" -print0 | xargs -0 sed -i 's/ href=\"_static\// href=\"\/warehouse\/_static\//g'
        find /home/runner/work/warehouse/warehouse/docs/build/ \( -type d -name .git -prune \) -o -type f -name "*.html" -print0 | xargs -0 sed -i 's/ src=\"_static\// src=\"\/warehouse\/_static\//g'
        touch /home/runner/work/warehouse/warehouse/docs/build/.nojekyll
# deploy to github pages
    - name: deploy documentation
      uses: JamesIves/github-pages-deploy-action@v4.8.0
      with:
        branch: gh-pages
        folder: /home/runner/work/warehouse/warehouse/docs/build/
        single-commit: true
